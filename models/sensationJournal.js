class SensationJournal {
  constructor(id, sensationId, journalId, intensity) {
    this.id = id;
    this.sensationId = sensationId;
    this.journalId = journalId;
    this.intensity = intensity;
  }
}

export default SensationJournal;
